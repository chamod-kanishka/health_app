name: health_app
description: "A new Flutter project."



environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.35.0
  cupertino_icons: ^1.0.6
  # audioplayers: ^1.2.0
  beep_player: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/health-care-logo.png
    - assets/beep.wav
